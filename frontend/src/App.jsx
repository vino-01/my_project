import React from "react";

export default function App() {
	return (
		<div style={{ padding: 24 }}>
			<h1>Secure File Share</h1>
			<p>
				Frontend placeholder. Use the API to upload a file, create a share
				link, and view it via the token.
			</p>
		</div>
	);
}

